{
  "name": "furbolt-sim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist && npm run move",
    "build": "vite build",
    "preview": "vite preview",
    "move": "node move.cjs"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.1.6"
  }
}